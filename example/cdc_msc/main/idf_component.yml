version: "0.0.1" # Component version, required only for components pushed to the service
targets: # List of supported targets (optional, if missing all targets are considered to be supported)
  - esp32s2
  - esp32s3
description: esp32-s3 CDC MSC example # Description (optional)
dependencies:
  # Required IDF version
  idf:
    version: ">=4.2"
  # For components maintained by Espressif:
  # Same as `espressif/component`
  tinyusb:
    git: https://github.com/iot-components/tinyusb.git
    public: true
